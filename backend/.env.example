# Backend environment variables example (DO NOT commit secrets)
MONGODB_URI=
JWT_SECRET_KEY=
PORT=8000
NODE_ENV=production
CLIENT_URL=
CLIENT_URL_2=

# Note: Add real values in your host's environment settings (Render/Heroku/etc.)
